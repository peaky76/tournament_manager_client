import React from "react";
import TeamsListItem from "./TeamsListItem";

const TeamsList = () => {
  return (
    <section>
      <ul>
        Teams
        <TeamsListItem />
      </ul>
    </section>
  );
};

export default TeamsList;
